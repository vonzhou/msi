
控制电视端，相当于Web项目；

-------

 多屏互动v1.2 目标：
    	数据库设计；
    	导航到某个主页后，单击开始按钮可以进入(这里仅仅是能够进入第一个页面)；
    	这里需要处理：从欢迎界面进入导航界面之间要进行初始化操作，如将currentURL 表滞空，
    	否则还没点击Enter就跳转了（历史遗留）
    	@2013.5.10
多屏互动v1.3 目标：
    	#加入stb.htm模块，用android端控制菜单切换。(看懂了，能加入但是这也是主页。)
    	#内容页面用js+div+css+ajax实现实现菜单切换效果
    	#难点：从android获得控制指令KeyUp Down 等如何实现正确响应（实时性）
    	解决方案一：用一个定时器，如果是更新的键值才会响应，以免受历史记忆的影响。
    	#进入内容页面后可以点击Enter键从而开始服务（视频，或者其他）
    	@2013.5.12-13
 多屏互动v1.4 目标：
    	#实现媒体推送功能。。。。。
    	
    	@2013.5.30-31