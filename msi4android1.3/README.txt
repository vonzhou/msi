手机端实现。
多屏互动v1.3 目标：
    	#数据库设计；
    	#导航到某个主页后，单击开始按钮可以进入(这里仅仅是能够进入第一个页面)；
    	这里需要处理：从欢迎界面进入导航界面之间要进行初始化操作，如将currentURL 表滞空，
    	否则还没点击Enter就跳转了（历史遗留）
    	#点击进入主页应该是个新的Activity(传递URL，重新load),界面只有主要的几个控制键。
    	#完成主页，退出等功能
    	注意：进入主页之前应该要必要的初始化工作。
    	
    	@2013.5.12